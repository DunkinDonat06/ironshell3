<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <title>IronShell Security Report</title>
    <style>
        body { font-family: Arial, sans-serif; margin: 2em; }
        h1 { color: #3b5998; }
        table { width: 100%; border-collapse: collapse; }
        th, td { border: 1px solid #ccc; padding: 8px; }
        th { background: #f3f3f3; }
    </style>
</head>
<body>
    <h1>IronShell Security Report</h1>
    {% for scanner, findings in data.items() %}
        <h2>{{ scanner }}</h2>
        <table>
            <tr>
                <th>Severity</th>
                <th>Title</th>
                <th>Description</th>
            </tr>
            {% for f in findings %}
            <tr>
                <td>{{ f.severity }}</td>
                <td>{{ f.title }}</td>
                <td>{{ f.description }}</td>
            </tr>
            {% endfor %}
        </table>
    {% endfor %}
</body>
</html>